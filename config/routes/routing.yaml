api_category_directory:
    resource: 'magazine/category/'
    type: directory

api_post_directory:
    resource: 'magazine/post/'
    type: directory

api_portal_directory:
  resource: 'magazine/portal/'
  type: directory

api_event_directory:
  resource: 'venue/event/'
  type: directory

api_comment_directory:
  resource: 'venue/comment/'
  type: directory

api_login_check:
    path: /login_check
